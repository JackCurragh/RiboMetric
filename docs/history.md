<!--
The changelog lives at the repository root so it reads correctly on GitHub.
`relative-docs` rewrites its `docs/...` links to resolve from this directory,
so the same file is correct in both places.
-->

```{include} ../CHANGELOG.md
:relative-docs: docs/
:relative-images:
```
